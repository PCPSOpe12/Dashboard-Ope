name: Deploy DPS Dashboard

on:
  push:
    branches: [main]
    paths:
      - 'data/export_dps.xlsx'
      - 'data/export_garde.xlsx'
      - 'data/export_competences.xlsx'
      - 'data/export_participations.xlsx'
      - 'data/requests/*.json'
      - 'index.html'
      - 'assets/**'
      - '.github/workflows/deploy.yml'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install openpyxl python-docx

      - name: Convert Excel to JSON
        run: python scripts/convert.py

      - name: Generate documents
        run: python scripts/generate_docs.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages
          exclude_assets: '.github,scripts,data/*.xlsx,data/requests'
